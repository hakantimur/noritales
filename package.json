{
  "name": "noritales-desktop-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Local NoriTales story and parent-voice experimentation studio",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test",
    "check": "node --check src/main.cjs && node --check src/core.cjs && node --check src/ui.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "electron": "44.2.0"
  }
}
